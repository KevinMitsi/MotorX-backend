# =============================================================
# Variables de entorno requeridas para desplegar MotorX en AWS
# Copia este archivo como .env y rellena los valores reales
# NUNCA subas el .env con valores reales al repositorio
# =============================================================

# -----------------------------------------------
# Base de datos - AWS RDS PostgreSQL
# -----------------------------------------------
DB_HOST=<tu-rds-endpoint>.rds.amazonaws.com
DB_PORT=5432
DB_NAME=motorx
DB_USER=<usuario-rds>
DB_PASSWORD=<password-rds>

# -----------------------------------------------
# JWT
# -----------------------------------------------
# Genera un valor seguro, al menos 256 bits (32+ caracteres)
# Ejemplo: openssl rand -base64 64
JWT_SECRET=<cambia-por-un-secreto-seguro-de-al-menos-256-bits>

# -----------------------------------------------
# Spring
# -----------------------------------------------
SPRING_PROFILES_ACTIVE=prod

# -----------------------------------------------
# Servidor
# -----------------------------------------------
PORT=8080

